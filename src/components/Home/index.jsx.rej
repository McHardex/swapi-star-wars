diff a/src/components/Home/index.jsx b/src/components/Home/index.jsx	(rejected hunks)
@@ -5,2 +5,2 @@ import HorizontalCard from '../shared/Cards/HorizontalCard';
-import Dropdown from 'react-bootstrap/Dropdown'
-import DropdownButton from 'react-bootstrap/DropdownButton'
+import Dropdown from 'react-bootstrap/Dropdown';
+import DropdownButton from 'react-bootstrap/DropdownButton';
@@ -17 +17 @@ class Home extends Component {
-    filterTitle: "filter"
+    filterTitle: 'filter'
@@ -50 +50 @@ class Home extends Component {
-  filterItems = (eventKey) => {
+  filterItems = eventKey => {
@@ -52,2 +52,4 @@ class Home extends Component {
-    const result = characters.filter(character => character.gender === eventKey);
-    this.setState({ 
+    const result = characters.filter(
+      character => character.gender === eventKey
+    );
+    this.setState({
@@ -85,4 +86,14 @@ class Home extends Component {
-            <DropdownButton id="dropdown-item-button" title={filterTitle} onSelect={this.filterItems}>
-              <Dropdown.Item as="button" eventKey="male">Male</Dropdown.Item>
-              <Dropdown.Item as="button" eventKey="female">Female</Dropdown.Item>
-              <Dropdown.Item as="button" eventKey="robot">Robot</Dropdown.Item>
+            <DropdownButton
+              id="dropdown-item-button"
+              title={filterTitle}
+              onSelect={this.filterItems}
+            >
+              <Dropdown.Item as="button" eventKey="male">
+                Male
+              </Dropdown.Item>
+              <Dropdown.Item as="button" eventKey="female">
+                Female
+              </Dropdown.Item>
+              <Dropdown.Item as="button" eventKey="robot">
+                Robot
+              </Dropdown.Item>
@@ -91 +102 @@ class Home extends Component {
-          { characters.length === 0 && <p>No result found</p>}
+          {characters.length === 0 && <p>No result found</p>}
